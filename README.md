# 🤖 Бот мониторинга финансовых новостей

Автоматический бот для мониторинга финансовых новостей через GNews API с отправкой переведенных статей в Telegram на украинском языке.

## 📋 Возможности

- ✅ Автоматический поиск новостей по финансовым темам (банкротство, банки, кредиты, законодательство)
- ✅ Поиск на трех языках: английский, украинский, русский
- ✅ Автоматический перевод всех новостей на украинский язык
- ✅ Отправка в Telegram с красивым форматированием
- ✅ Ссылка на оригинальную статью
- ✅ Работает 24/7 с настраиваемым интервалом проверки
- ✅ Отслеживание дубликатов (не отправляет одну новость дважды)

## 🚀 Деплой на Render

### 1. Подготовка Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Создайте нового бота командой `/newbot`
3. Сохраните полученный **токен бота**
4. Узнайте свой **Chat ID** через [@userinfobot](https://t.me/userinfobot)

### 2. Деплой на Render

1. Создайте аккаунт на [render.com](https://render.com)
2. Нажмите **New +** → **Background Worker**
3. Подключите ваш GitHub репозиторий
4. Настройте сервис:
   - **Name**: `news-monitor-bot` (или любое другое имя)
   - **Environment**: `Python 3`
   - **Build Command**: оставьте пустым
   - **Start Command**: `python news_bot.py`

5. Добавьте переменные окружения (Environment Variables):
   - `GNEWS_API_KEY` = `8ad6c10d7880908151746a4b48513c4d`
   - `TELEGRAM_BOT_TOKEN` = ваш токен от @BotFather
   - `TELEGRAM_CHAT_ID` = ваш Chat ID
   - `CHECK_INTERVAL_MINUTES` = `60` (опционально)
   - `CHECK_HOURS` = `1` (опционально)

6. Нажмите **Create Background Worker**

### 3. Мониторинг

После деплоя вы можете:
- Просматривать логи в реальном времени на Render
- Получать уведомления в Telegram о найденных новостях
- Перезапускать сервис через дашборд Render

## 📁 Структура проекта

```
test_gnews/
├── news_bot.py         # Единый файл с ботом (все модули объединены)
├── .env                # Переменные окружения (для локального запуска)
├── requirements.txt    # Зависимости Python
├── render.yaml         # Конфигурация для автоматического деплоя
└── README.md           # Эта документация
```

## 🖥️ Локальный запуск

### Установка

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd test_gnews

# Python 3.10+ уже включает все необходимые библиотеки
# Дополнительные зависимости не требуются!
```

### Настройка

Отредактируйте файл `.env`:

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
CHECK_INTERVAL_MINUTES=60
CHECK_HOURS=1
```

### Запуск

```bash
# Windows
python news_bot.py

# Linux/Mac
python3 news_bot.py
```

## ⚙️ Настройки

Все настройки через переменные окружения:

| Переменная | Описание | По умолчанию |
|-----------|----------|--------------|
| `GNEWS_API_KEY` | API ключ GNews | Уже настроен |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | **Обязательно** |
| `TELEGRAM_CHAT_ID` | ID чата Telegram | **Обязательно** |
| `CHECK_INTERVAL_MINUTES` | Интервал проверки (мин) | 60 |
| `CHECK_HOURS` | За сколько часов искать | 1 |
| `LOG_LEVEL` | Уровень логирования | INFO |

## 📱 Формат сообщений

Каждая новость отправляется в формате:

```
📰 Банкрутство

Заголовок статьи (на украинском)

Описание статьи (на украинском)...

Джерело: Reuters
Дата: 26.11.2025 14:30

📎 Читати оригінал статті
```

## 🔍 Темы мониторинга

Бот отслеживает новости по 14 запросам:

- **Банкрутство**: bankruptcy (EN), банкрутство (UK), банкротство (RU)
- **Банки**: banks (EN), банки (UK/RU)
- **Кредити**: credits, loans (EN), кредити (UK), кредиты (RU)
- **Законодавство**: legislation, law (EN), законодавство (UK), законодательство (RU)

## 🛠 Технологии

- Python 3.10+ (только стандартные библиотеки)
- GNews API (для поиска новостей)
- Google Translate API (для перевода)
- Telegram Bot API (для отправки сообщений)

## ❓ Решение проблем

### Бот не запускается на Render

1. Проверьте логи в разделе "Logs" на Render
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что токен бота и Chat ID корректны

### Не приходят сообщения в Telegram

1. Отправьте боту команду `/start`
2. Проверьте правильность `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID`
3. Убедитесь, что бот работает (проверьте статус на Render)

### Нет новых статей

- За указанный период может не быть новых публикаций
- Увеличьте `CHECK_HOURS` для проверки за больший период
- Проверьте лимиты GNews API

## 📊 Производительность

- **Запросов за проверку**: 14 (по числу тем × языков)
- **Задержка между запросами**: 0.5 сек
- **Кэш**: очищается каждые 24 часа
- **Потребление памяти**: ~50-100 MB

## 🔒 Безопасность

- Не коммитьте `.env` файл в Git (добавьте в `.gitignore`)
- Используйте переменные окружения для токенов
- На Render все переменные хранятся безопасно

## 📄 Лицензия

Свободное использование для личных и коммерческих целей.

---

**Создано для мониторинга финансовых новостей 24/7 🚀**
